---
title: Get Registry Function Org
description: >-
  Lists all the functions in the given `org`. If the session is scoped to the
  same `org`, functions that are not public will be returned, otherwise only
  public functions will be returned.
full: true
_openapi:
  method: GET
  route: /registry/function/{org}
  toc: []
  structuredData:
    headings: []
    contents:
      - content: >-
          Lists all the functions in the given `org`. If the session is scoped
          to the same `org`, functions that are not public will be returned,
          otherwise only public functions will be returned.
---

<APIPage document={"./content/scale/openapi-v1.json"} operations={[{"path":"/registry/function/{org}","method":"get"}]} hasHead={false} />