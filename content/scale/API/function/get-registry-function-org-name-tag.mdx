---
title: Get Registry Function Org Name Tag
description: >-
  Retrieves a function from the given `org` given its `name` and `tag`. If the
  session is scoped to the same `org`, functions that are not public will be
  returned, otherwise only public functions will be returned.
full: true
_openapi:
  method: GET
  route: /registry/function/{org}/{name}/{tag}
  toc: []
  structuredData:
    headings: []
    contents:
      - content: >-
          Retrieves a function from the given `org` given its `name` and `tag`.
          If the session is scoped to the same `org`, functions that are not
          public will be returned, otherwise only public functions will be
          returned.
---

<APIPage document={"./content/scale/openapi-v1.json"} operations={[{"path":"/registry/function/{org}/{name}/{tag}","method":"get"}]} hasHead={false} />