---
title: Get Registry Signature Org Name Tag
description: >-
  Retrieves a signature from the given `org` given its `name` and `tag`. If the
  session is scoped to the same `org`, signatures that are not public will be
  returned, otherwise only public signatures will be returned.
full: true
_openapi:
  method: GET
  route: /registry/signature/{org}/{name}/{tag}
  toc: []
  structuredData:
    headings: []
    contents:
      - content: >-
          Retrieves a signature from the given `org` given its `name` and `tag`.
          If the session is scoped to the same `org`, signatures that are not
          public will be returned, otherwise only public signatures will be
          returned.
---

<APIPage document={"./content/scale/openapi-v1.json"} operations={[{"path":"/registry/signature/{org}/{name}/{tag}","method":"get"}]} hasHead={false} />